# Bar Capacity Mockup

Interactive mockup for a door-count app: live add/remove counter, undo,
shift reset, and a manager audit view of past nights.

## Run locally
npm install
npm run dev

## Deploy
Push to GitHub, then import the repo in Netlify (build command: `npm run build`,
publish directory: `dist`). Netlify will auto-deploy on every push.
